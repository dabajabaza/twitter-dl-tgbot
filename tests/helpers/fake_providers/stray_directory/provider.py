"""A folder somebody started a Provider in and never finished.

No __init__.py, so the module scanner does not report it at all — which is why
discovery looks for directories itself. Left unfinished on purpose.
"""
